type = "reftest_plugin"
name = "Reference test plugin"
version = "1.0.0"
description = "Sentinel-writing plugin used by the kernel acceptance test to exercise discovery, assignment, dispatch, and safety-net resync."
config_schema = "Config"
